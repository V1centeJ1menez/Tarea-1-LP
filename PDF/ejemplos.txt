• codigo.txt
DEFINE $_Largo
DEFINE $_Ancho
DEFINE $_Area
DEFINE $_Texto
DP $_Largo ASIG 6
DP $_Ancho ASIG 5
DP $_Area * $_Largo $_Ancho
DP $_Texto ASIG #La altura del rectangulo es #
DP $_Texto + $_Texto $_Area
MOSTRAR($_Texto)
• output.txt
La altura del rectangulo es 30

• codigo.txt
DEFINE $_NumeroUno
DEFINE $_NumeroDos
DP $_NumeroUno ASIG 670
DP $_NumeroDos ASIG 670
DEFINE $_Cond
DP $_Cond > $_NumeroUno $_NumeroDos
if ($_Cond) {
    DEFINE $_Texto
    DP $_Texto ASIG #Numero Uno es mayo a Numero Dos#
    MOSTRAR($_Texto)
} else {
    DP $_Cond == $_NumeroUno $_NumeroDos
    if ($_Cond) {
        DEFINE $_Texto
        DP $_Texto ASIG #Numero Uno es igual a Numero Dos#
        MOSTRAR($_Texto)
    } else {
        DEFINE $_Texto
        DP $_Texto ASIG #Numero Uno es menor a Numero Dos#
        MOSTRAR($_Texto)
    }
}
• output.txt
Numero Uno es igual a Numero Dos